# 🌳 Git・GitHub ブランチ（Branch）体験チュートリアル

このファイルは、明日行う「ブランチ（枝分かれ）」と「プルリクエスト（合流）」の体験手順をまとめたものです。

---

## 📅 体験テーマ
**「実験レポートのテンプレート機能を追加する」**
メインブランチ（本番用の安定版）を汚さずに、新しいアイデアを試すための「実験室（ブランチ）」を作り、最終的に合流させる一連の流れを体験します。

---

## 💻 体験ステップ一覧

### ステップ1：新しいブランチ（実験室）を作って移動する
新しく `feature/template` という名前のブランチを作ります。
```powershell
git checkout -b feature/template
```
> 💡 **解説**: これで本番の `main` から枝分かれした安全な実験用ブランチに切り替わりました。

---

### ステップ2：実験室でファイルを作り、コミット（保存）する
1. フォルダ内に新しく **`template.md`** という名前のファイルを作成します。
2. ファイルの中に、以下のような実験レポートのテンプレート（型）を書いて保存します：
   ```markdown
   # 実験レポート テンプレート
   
   ## 1. 目的
   - ここに目的を記載
   
   ## 2. 実験方法
   - 手順を記載
   ```
3. この変更を、実験ブランチ上に登録・保存します。
   ```powershell
   git add template.md
   git commit -m "実験レポートのテンプレートを追加"
   ```

---

### ステップ3：実験ブランチをそのままGitHubにプッシュ（送信）する
「こんな新しいアイデア（実験ブランチ）を作ったよ」とGitHubに送ります。
```powershell
git push -u origin feature/template
```

---

### ステップ4：GitHub上でマージ（合流）させる（★一番の見どころ！）
1. ブラウザで [GitHubリポジトリページ](https://github.com/takanit/gemini_Labo_report_comment) を開きます。
2. 画面上部に自動的に表示される黄色の帯の **「Compare & pull request」** ボタンをクリックします。
3. 合流申請画面（Pull Request）が開くので、内容を確認して **「Create pull request」** をクリックします。
4. 問題がないことを確認し、緑色の **「Merge pull request」** ➡ **「Confirm merge」** をクリックします。
> 🌟 **結果**: これで、GitHub上の `main` ブランチに実験成果が美しく合流しました！

---

### ステップ5：手元の `main` も最新状態にする（現代への取り込み）
GitHub上の `main` が新しくなったので、自分のパソコンの `main` にもその変更を取り込みます。

1. パソコンのターミナルで、メインブランチに戻ります。
   ```powershell
   git checkout main
   ```
   *(※この時点では、手元の `main` にはまだ `template.md` はありません)*
2. GitHubから最新状態を引っ張ってきます。
   ```powershell
   git pull
   ```
   👉 手元のフォルダに `template.md` が出現し、ローカルも最新の `main` に更新されます！

---

### 🛠️ 知っておくと便利な関連コマンド
- **今あるブランチと自分の居場所を確認する**:
  ```powershell
  git branch
  ```
- **既存のブランチを切り替える（タイムトラベルと同じ）**:
  ```powershell
  git checkout <ブランチ名>
  ```
